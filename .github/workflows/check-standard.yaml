name: BioGit-R-CMD-check
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
env:
  R_VERSION: 4.2.1
jobs:
  build:
    runs-on: [ ubuntu22 ]
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      GXPA_TOKEN: ${{ secrets.GXPA_TOKEN }}
      GXPA_USERNAME: ${{ secrets.GXPA_USERNAME }}
      GXPA_PASSWORD: ${{ secrets.GXPA_PASSWORD }}
      R_KEEP_PKG_SOURCE: yes
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-pandoc@v2
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.2.1'
          extra-repositories: http://pm.rdcloud.bms.com/bms-cg-biogit-bran/latest
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check
          dependencies: '"hard"'
      - uses: r-lib/actions/check-r-package@v2
