% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uploadfile.R
\name{send_file_to_session}
\alias{send_file_to_session}
\title{Push a File to a GXPA Session}
\usage{
send_file_to_session(
  session_id,
  filename_local,
  file_save_name,
  user_cookie = login_and_get_user_cookie()
)
}
\arguments{
\item{session_id}{session id from a session that has already been opened in GXPA}

\item{filename_local}{input file name, with path, to import}

\item{file_save_name}{file name to be saved as in GXPA. Must match a know name (see details)}

\item{user_cookie}{user cookie (default is using \code{\link[=login_and_get_user_cookie]{login_and_get_user_cookie()}})}
}
\value{
message indicating upload was successful
}
\description{
Importing data for an already opened GXPA session.
}
\details{
The GXPA session needs to exist, so go to the
\href{https://geneatlas.redda.celgene.com/sessions/}{GXPA app} to see open
sessions and create a new one if needed. The session id can be found on the
main sessions page or in the URL once in the session
(6 uppercase chars or numbers)

Common \code{file_save_name} values are expr.expr.txt, expr.samples.csv,
expr.series_info.csv, and expr.score_info.csv, for expression data loading, and
GEO.expr.txt, GEO.samples.csv, GEO.series_info.csv, and GEO.score_info.csv,
for GEO data loading
}
\examples{
\dontrun{
  tmp_file <- tempfile()
  utils::write.csv(matrix(rnorm(100), nrow = 10), tmp_file)
  send_file_to_session(
    session_id = '7AR22L',
    filename_local = tmp_file,
    file_save_name = 'expr.expr.txt')
}
}
