% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uploadfile.R
\name{remove_session}
\alias{remove_session}
\title{Remove session directory}
\usage{
remove_session(session_id, user_cookie = login_and_get_user_cookie())
}
\arguments{
\item{session_id}{the id of session}

\item{user_cookie}{the cookie string. Can use
\code{\link[=login_and_get_user_cookie]{login_and_get_user_cookie()}} to get it. If you don't provide the cookie
string, then the function will run login_and_get_user_cookie(). So, if
you provide the cookie then it saves one interaction with the server.}
}
\value{
Invisibly returns the full html output which can be useful in
case you didn't get a success message.
}
\description{
Remove a session directory. This is permanent. It will fail
if the session is linked to a series. To unlink a session and a series,
you need to use the web interface to delete the series. Then you can remove
the session directory.
}
\examples{
\dontrun{
user_cookie = login_and_get_user_cookie()
sess_id = begin_new_session("new_test_session", user_cookie=user_cookie)
remove_session(sess_id)
}

}
\seealso{
\code{\link[=begin_new_session]{begin_new_session()}}, \code{\link[=send_file_to_session]{send_file_to_session()}},
\code{\link[=dry_run_session]{dry_run_session()}}, \code{\link[=load_session]{load_session()}},
\code{\link[=remove_session]{remove_session()}}
}
